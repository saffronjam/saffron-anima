# Build output
/build/
/out/
/install/
/appdata/
engine/target/
# Vendored Jolt 5.3.0 source — compiled by saffron-physics-sys/build.rs.
# Ignored to keep it out of review; to become a download-on-demand build target (PP-11).
engine/crates/physics-sys/vendor/
# ts-rs scratch output from `#[ts(export)]` `cargo test` runs; the real editor TS is
# emitted by `xtask gen-protocol` (10-protocol-codegen phase 5), not these.
engine/crates/protocol/bindings/

# IDE
.cache/
.vs/
.vscode/
.idea/
*.user

# OS
.DS_Store
